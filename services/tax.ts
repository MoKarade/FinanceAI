// Namespace canonique pour la logique fiscale canadienne.
// Le contenu reel reside encore dans utils/tax.ts ; ce fichier sert
// d'alias stable pour les consumers (composants React + futur MCP server).
// Migration physique prevue dans un commit ulterieur.
export * from '../utils/tax';
